#1. 클래스 = 객체(object) + 메서드
#클래스는 객체(데이터)와 메서드(동작)를 포함하는 설계도입니다.
#객체는 그 클래스로부터 생성된 인스턴스를 의미합니다. 즉, 클래스로부터 개별적으로 만들어진 실체가 인스턴스입니다.
#2. 클래스 내부에서 객체는 인스턴스라고 부름
#클래스로부터 만들어진 각각의 객체는 인스턴스라고 부릅니다.
#클래스는 하나지만, 여러 개의 인스턴스를 만들 수 있습니다. 각 인스턴스는 독립적으로 작동하며, 고유한 데이터를 가질 수 있습니다.
#3. 클래스 안에는 여러 종류의 변수와 메서드가 있음
#클래스 변수: 클래스 자체에 속하며, 모든 인스턴스가 공유하는 변수.
#인스턴스 변수: 각 인스턴스가 고유하게 가지는 변수. 인스턴스마다 다르게 설정됩니다.
#클래스 메서드: 클래스 자체에서 호출되는 메서드로, @classmethod를 사용하고 cls를 첫 번째 인자로 받습니다.
#인스턴스 메서드: 인스턴스에서 호출되는 메서드로, 첫 번째 인자로 self를 받습니다.
#4. 인스턴스 생성 시 self 사용
3클래스에서 인스턴스를 만들 때, 인스턴스 메서드는 **해당 인스턴스(자기 자신)**를 참조하기 위해 **self**를 사용합니다.
#__init__ 메서드는 초기화 메서드로, 인스턴스가 생성될 때 호출되어 인스턴스 변수를 초기화합니다.
#예: self.hello = "어쩌구"는 인스턴스가 생성될 때, 그 인스턴스의 hello라는 변수를 "어쩌구"로 설정하는 것.
#5. self.hello는 인스턴스 변수
#self.hello에서 self는 해당 인스턴스(객체)를 의미합니다. 즉, self.hello는 인스턴스가 가지고 있는 hello라는 변수를 참조하게 됩니다.
#6. 메서드에서 인스턴스 변수 호출
#def greeting(self):에서, self.hello를 호출할 때, 해당 인스턴스가 가진 hello 변수를 사용합니다.
#예: print(self.hello)는 해당 인스턴스의 hello 변수를 출력하는 동작입니다.


#class 클래스 이름:
    #def __init__(self):
        #self.속성 = 값


# 클래스 내부에서 속성 만들기
class Person:
    def __init__(self):
        self.hello = '안녕하세요'

    def greeting(self):
        print(self.hello)

# 클래스 외부에서 속성 호출하기
james = Person()  #Person 클래스 자체를 james 인스턴스 변수에 넣고 아래의
james.greeting()  #Greeting에서 self.hellp를 호출 할 때, 해당 인스턴스가 가진 hello 변수를 사용해서 "안녕하세요"가 출력


# 속성(여기서 hello)은__init__메서드에서 만드는 점과 self.(점)을 붙인 뒤 값을 할당하는 것이 포인트.!
# 클래스 안에서 속성을 사용할 때도 self.hello처럼 self에 점을 붙여서 사용한다.(점은 여기서 접근한다는 뜻이다)
# __init__ 메서드의 역할:
#__init__ 메서드는 특별한(스페셜) 메서드로, 클래스의 인스턴스가 생성될 때 자동으로 호출되는 메서드입니다.
#__init__의 역할은 객체를 **초기화(initialize)**하는 것입니다. 클래스가 인스턴스로 만들어질 때, 그 객체가 어떤 데이터를 가질지 초기 설정을 해줍니다.
#__init__ 메서드는 james = Person()처럼 클래스에 ()괄호를 붙여서 인스턴스를 만들 때 호출되는 특별한 메서드
#__init(initalize)이라는 이름 그대로 인스턴스(객체)를 초기화함

#앞 뒤로 __(밑줄 두 개)가 붙은 메서드는 파이썬이 자동으로 호출해주는 메서드인데 스폐셜 메스더 또는 매직 메서드라고 하며,
#파이썬의 여러 가지 기능을 사용할 때 이 스폐셜 메스드를 채우는 식으로 사용하게 됨.
#스페셜 메서드 또는 매직 메서드란?
#**__init__**처럼 앞뒤로 두 개의 밑줄(__)이 붙은 메서드들을 스페셜 메서드 또는 매직 메서드라고 부릅니다.
#이러한 스페셜 메서드는 파이썬이 특정 상황에서 자동으로 호출해주는 메서드입니다.
#예: __init__는 클래스의 인스턴스가 생성될 때 자동으로 호출됩니다.
#또 다른 예로, __str__ 메서드는 print(객체)를 호출할 때 객체의 문자열 표현을 반환합니다.
#. 스페셜 메서드의 종류:
#__init__: 객체가 생성될 때 호출되어 인스턴스를 초기화합니다.
#__str__: print()나 str() 함수를 호출할 때 객체를 문자열로 표현하는 메서드입니다.
#__len__: len() 함수가 객체의 길이를 반환할 때 호출됩니다.
#이 외에도 스페셜 메서드는 매우 많으며, 파이썬의 다양한 기능을 구현할 때 사용됩니다.


#self의 의미
#self는 인스턴스 자기 자신을 의미
#우리는 인스턴스가 생성될 때 self.hello = '안녕하세요'처럼 자기 자신에 속성을 추가함
# __init__의 매개변수가 self에 들어가는 값은 Person()이라고 할 수 있음 > self가 완성된 뒤 james에게 할당 됨
# 호출하면 현재 인스턴스가 자동으로 매개변수 self에 들어옴.
# greeting 메서드에서 print(self.hello)처럼 속성을 출력할 수 있었던 것임
